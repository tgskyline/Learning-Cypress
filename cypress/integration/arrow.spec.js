/// <reference types = "cypress" />


it('nada agora', function () { })
    function soma(a, b) {
       return a + b;
    }

    //const soma = (a, b) => {
      //  return a+b
    //}

    //const soma=(a,b) => a+b

    //const soma=(a) =>  a+a

    //const soma=a=>a+a

    //const soma=()=> 5+5

  //comentário melhorado
  
    console.log(soma(1,4))

    